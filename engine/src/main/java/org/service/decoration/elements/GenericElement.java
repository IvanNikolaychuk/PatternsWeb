package org.service.decoration.elements;

/**
 * Default element wrapper. This could be any element that is not defined as some logical block.
 */
public class GenericElement extends CodeElement {
    public GenericElement(String word) {
        super(word);
    }
}
